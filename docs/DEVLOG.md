# 开发变更记录（DEVLOG）

> 本文件是本项目**长期维护**的变更台账，面向多 Agent / 多人协作。
> 每次修改代码后、提交前，必须在本文件末尾追加一条记录。历史条目只增不改。
> 架构级决策另见 `docs/adr/`；本文件只记录"改了什么、为什么、验证到哪一步"。

---

## 一、记录规则

### 1.1 必须记录的改动范围

- `csrc/` —— CUDA / C++ 扩展
- `ampere_kv/` —— 引擎 Python 代码
- `triton_kernels/`、`scripts/`、`configs/`、`benchmarks/`、`tests/`
- `setup.py`、`Makefile`、`pyproject.toml` —— 构建与依赖

纯文档改动（README、docs）可记可不记。**任何新的性能数字、正确性结论、失败结论都必须记录**——后续 Agent 依赖这些结论确定基线，重复实验和"编造加速"都是禁止的。

### 1.2 记录模板

```markdown
### [YYYY-MM-DD] 一句话标题
- **作者**：Agent 名称 / 人
- **阶段/分支**：
- **动机**：为什么必须改，不改会怎样
- **修改的模块与文件**：`路径:行号`，逐条列出
- **改动要点**：改了什么，为什么用这个方案
- **验证状态**：跑通的命令与结果；未验证的部分必须显式写"未验证"
- **性能影响**：有数据写数据；没有写"未测量"
- **遗留问题 / 下一步**：
```

### 1.3 写法要求

- **显式区分"已验证"与"未验证"**：这是本项目文档的一贯口径（如"不代表精度验收""不代表长期无泄漏"）。不要用一次短上下文运行冒充完整验收。
- **只写事实**：没测到的数字不写，推断必须标注"推断，未测量"。
- 新记录追加在**文件末尾**，按时间正序，便于 diff 定位。

---

## 二、变更记录

### 2026-09-20 Prefill融合写入与诊断精简（待验证）

- 上一版单Token融合的算子与模型对照已有通过反馈。3090短输入固定32输出：BF16/INT8 TTFT为61.787/88.986 ms，TPOT为48.879/47.611 ms，总耗时1577.027/1565.429 ms。仅三次中位数，不宣称稳定加速。
- 本轮沿用已有未提交的批量写入实现并补齐边界：每(Token, KV头)一个warp，GPU块表映射物理位置；支持非对齐批量追加，Decode复用写入返回的块表。
- 量化规则与BF16 Prefill Attention不变；int8-check准备阶段也使用融合写入。补充二维网格上限、64位位置及写入设备端块号断言，保留读取入口的块号保护。
- 精简模型诊断中的逐层完整KV读回、重复地址/块归属检查及旧中间返回值；结构检查集中到加载后，移除每层position_ids.item。保留数值有限性、logits/Token对照与缓存长度。
- 沿用六个算子场景覆盖整段写入、非对齐追加、Prefill后单Token跨块Decode、整数/scale及未写哨兵；没有增加新测试框架。
- 本轮同时减少两种精度的公共检查开销，新旧性能差异不能全部归因于Prefill融合。只做本地静态检查，未编译运行，需云端重建后验证。

### 2026-09-20 补充勘误与实现状态

- 以下旧记录保留作为历史；用户已确认3090上INT8延迟高于BF16，尚未收录完整INT8数字，不估算减速比例。
- 旧文72/216只统计部分GPU标量取回点，遗漏每层位置检查及选词；item也不等同于全设备同步。参考量化每Decode额外144处取回点是代码事实，但约1300个kernel、25/250微秒和增加18毫秒不是实测归因。
- KV字节占比不能替代时间占比；只能省4微秒、必须KV字节追平权重才有收益均未被证明。INT8读取内核额外开销尚未测量，不能断言可忽略。
- 容量勘误：INT8 K/V数据256字节，K/V scale合计10字节，共266字节；BF16为512字节。比例约1.925，scale相对整数数据开销3.91%，占总量3.76%，不是7.8%。
- 本轮在现有CUDA文件融合单Token量化与分页写入，每KV头一个warp；扩展注册后由paged_cache的fused参数调用，runner仅在INT8 CUDA Decode启用，Prefill和CPU参考路径不变。
- K每32维、V每128维的规则不变；保存FP16 scale后再量化，零scale特例、2^-14下限、最近偶数舍入及[-127,127]范围保持不变。
- 快路径要求输入有限且scale可表示，不提供非法数值的同步拒绝保证；仍检查元数据，GPU失败后请求必须丢弃。量化写入无GPU标量取回，读取块号与位置检查未改。
- 复用六个算子场景逐Token写入，严格检查整数/scale、零向量、极小值、半整数、跨块及未写哨兵，不新增测试框架。
- 仅通过本地静态检查，未编译或运行。需重新构建并验证算子、模型、性能；新实现收益未测量，不保证快于BF16。

### [2026-09-17] INT8 KV 性能分支：定位"量化后延迟反而更高"的根因

- **作者**：平台q（只读诊断，未改动代码）
- **阶段/分支**：测量性能；`main` @ `8fd1075`
- **动机**：RTX 3090 上执行 `--mode benchmark --cache paged --kv-dtype int8`，INT8 KV 的延迟高于 BF16 CUDA，需定位原因并决定该分支后续走向。
- **修改的模块与文件**：无。本次仅阅读代码与运行输出，未产生文件改动。
- **改动要点（诊断结论，三条互相独立）**：
  1. **收益上限近乎为零**：Qwen3-8B BF16 权重约 16.4 GB，每个 Token 必须完整读一遍；52 Token 上下文下 attention 的 K/V 读取仅约 7.7 MB，占 0.047%。Decode 是权重带宽与启动开销受限，不是 KV 带宽受限。即使 KV 读取降到 0，每个 Token 也只省约 4 µs，而实测 TPOT 为 48819 µs（Amdahl 定律：加速对象本身不是瓶颈）。
  2. **反而变慢来自热路径上的参考实现**：`PagedKVCache.append`（`ampere_kv/paged_cache.py:74-82`）在每层每 Token 调用 `quantize_kv`，而该函数在 `ampere_kv/quantization.py:1,11-12` 中自我声明为"不接入缓存、不用于性能计时"的参考实现。它内部含两处 `.item()` 设备同步（`quantization.py:17,27`）与约 18 个逐元素小 kernel。36 层 × K/V = 每个 Token 约 1300 次额外 kernel 启动 + 144 次**额外**设备同步。

     **BF16 路径本来就有设备同步**：`paged_decode_impl` 的块号检查（`csrc/paged_decode.cu:199-202`）每次调用 `min().item()` / `max().item()` 各一次，两个精度入口共用同一段代码，因此 BF16 每 Token 同步 72 次、INT8 216 次。差别不在于同步的有无，而在于**同步之后 GPU 有多少活可干**：

     - BF16 每层的块号检查之后，紧跟着约 50 个算子、约 1.0 ms 的真实 GPU 计算，CPU 的 Python 派发被掩盖在 GPU 执行时间内；
     - INT8 每层被 6 次同步切成 5 段，其中量化区域操作的是 1024 元素的小张量，GPU 实际计算约 25 µs，而 PyTorch 派发那 18 个算子要约 250 µs——**GPU 空转等 CPU**。

     按每层两次量化、每次约 250 µs 空转粗估，整 Token 约 +18 ms（**代码推断，未测量**；精确拆分需要 profiler 或逐区域计时）。
  3. 次要：INT8 内核每 Token 多出 5 次 shuffle + 8 次 cvt + 8 次 fmul（`csrc/paged_decode.cu:67,92`），量级可忽略；**内核未见退化证据，但项目从未单独测过 INT8 内核**（`ampere_kv/paged_decode.py:150` 注明 `--benchmark` 不测 INT8）。
- **容量事实**：计入 FP16 scale 后，INT8 KV 相对同容量 BF16 约 **1.93×**（scale 占 7.8% 开销）。当前 INT8 的真实收益是**容量**，不是单请求延迟。
- **验证状态**：结论来自代码审查 + 用户提供的 3090 运行输出。BF16 侧为实测数据（TTFT 中位数 73.862 ms，TPOT 中位数 48.819 ms，总耗时中位数 1586.500 ms，输出吞吐中位数 20.170 Token/s）。**INT8 侧完整计时数字尚未取得；"变慢约 +18 ms/Token"为代码推断，未测量。**
- **性能影响**：本次无代码改动。
- **遗留问题 / 下一步**（对齐方案文档第 1556 行"保留容量贡献，分析临界 Context；优化融合而非编造加速"）：
  1. 给 `paged_decode --benchmark` 补 INT8 算子级基线（length=64/256/1024），把"内核账"与"端到端账"分开。
  2. 去掉热路径上的 `.item()` 同步（至少移出计时路径），让 INT8 不再倒亏。
  3. 实现方案第 681 行的 V1 量化写入融合，把每 Token 72 次参考量化调用 + 144 次额外同步压成 36 次内核启动，量化侧同步归零（块号检查的 72 次共用同步属另一项优化，需单独处理）。
  4. 扫描 Context 找收益边界；对 8B / 3090 单请求，KV 字节追平权重字节约在十万 Token 量级，远超本机容量——这本身就是结论。
- **禁止事项**：不得把本分支结论写成"INT8 更快"。

### 2026-09-20 BF16批量分页写入与Prefill路径对齐（待云端验证）

- **作者**：平台q（只改代码，本机无CUDA，未编译未运行）
- **动机**：上一轮云端3090实测（输入39 Token、输出32，提交`918350e`）BF16/INT8 TTFT为106.497/49.657 ms。当时两条Prefill路径在写入与读回方式上不同：INT8走融合批量写入、SDPA直接用原始BF16 K/V；BF16走参考`append`的逐Token Python复制加`get()`的逐Token切片读回。本轮消除这一实现差异，使两条路径的写入与读回方式相同；**剩余差异仍涉及量化与反量化、KV数据量等，需配对实验验证，本轮不给出归因结论**。上面"不得写成INT8更快"的禁令仍然有效。
- **改动**：`csrc/paged_decode.cu`新增`bf16_write_kernel`与`bf16_write_cuda`，布局与分工同量化写入（每(Token, KV头)一个warp、32线程覆盖128维），按位复制不经FP32往返；保留设备端块号断言、二维网格上限、64位索引、当前设备与当前stream，只检查CPU可见元数据。`csrc/bindings.cpp`注册`bf16_write`。
- `ampere_kv/paged_cache.py`融合分支去掉"仅INT8"限制，按`_key_scale`是否为空选择写入入口；块分配、块表上传与返回值两种精度共用。扩展入口存在性、每头128维与KV头数不超过1024都在修改块表之前拒绝，避免登记了未写入的Token；这些条件与CUDA写入入口的检查同源，不另建回滚框架。非融合参考路径与CPU路径不变。
- `ampere_kv/runner.py`：CUDA分页且块16、每头128维时，BF16/INT8 Prefill共用"先批量写缓存、再用原始BF16 K/V做SDPA"，**BF16不再从分页存储读回全部K/V**；CUDA Decode的单Token追加两种精度都走批量入口并复用返回块表，随之失效的`table is None`回退已删除。GQA映射、causal掩码、scale、dropout、RoPE、残差、MLP、选词与计时边界均未改；连续缓存与非支持设备仍走参考实现。
- `ampere_kv/paged_decode.py`新增`check_bf16_write`并接入非benchmark入口：两头整段17 Token、八头分批(15, 32, 33)，覆盖空缓存整段写入、非对齐批量追加、跨块单Token追加与非连续物理块映射；核对写入值与输入逐元素相同、既有内容不被破坏、未写位置保持NaN哨兵、返回块表与有效长度正确、块最终归还。六个INT8场景与阈值未改，benchmark计时口径未改。
- **验证状态**：本地仅做AST解析、`git diff --check`与CUDA声明/定义/绑定/参数/索引静态核对，**未编译、未运行任何GPU测试**。需云端`make build`后依次跑`python -m ampere_kv.paged_decode`、`python -m ampere_kv.runner --mode check --cache paged`（对HF严格对照，会实际走新BF16写入路径）、`--mode cuda-check`与`--mode benchmark`。**性能未测量，不承诺加速幅度**；本轮收益含"移除BF16 Prefill分页读回"，不能全部归因于存储精度或单个内核。
- **输入长度事实**：用户在同一台3090上更换了输入提示词，本轮输入Token数为39、比上一轮增加，因此本轮与上一轮的TTFT不能直接比较；BF16 TTFT由61.787到106.497 ms的变化不能全部归因于输入长度，本轮也没有做同输入的配对测量。后续每轮benchmark记录输入Token数，便于判断跨轮可比性。
- **遗留问题**：Decode侧INT8相对BF16仍有约1.4 ms/Token净差，来源未测量；`--mode cuda-check`的Prefill严格对照两侧现在走同一路径，不再能区分新旧BF16写入，该保障由`check_bf16_write`的逐元素对照承担。

### 2026-09-20 补记：等路径配对实测的条件与数字

- 上一节改动在提交`83954a2`推送后由用户在云端编译运行；此前只写了"待云端验证"，现补记实际条件与数字。
- **条件**：RTX 3090，Qwen3-8B（revision `b968826d`），缓存=paged，Decode=CUDA V0，Prefill=BF16 SDPA；输入28 Token（用户指定的固定提示词），忽略EOS固定输出32 Token；预热1次、测量3次取中位数；每层物理容量64 Token，最终KV长度60，Decode跨块。
- **结果（中位数）**：BF16 TTFT 45.634 ms、TPOT 39.045 ms、总耗时1256.041 ms、输出吞吐25.477 Token/s；INT8 TTFT 45.165 ms、TPOT 38.969 ms、总耗时1253.215 ms、输出吞吐25.534 Token/s。组内离散：TTFT 0.609/0.437 ms，TPOT 0.214/0.284 ms。
- **可记录的结论（经Codex复核后收窄口径）**：在已经测量的硬件、输入长度和固定输出工作量下，等路径比较**未观察到INT8的延迟收益**；计入scale后，同Token容量的BF16/INT8 KV存储字节比为**1.9248**。两条差值（TTFT 0.469 ms、TPOT 0.076 ms）都小于组内离散，n=3。
- **不得写成两个更强的结论**：①"INT8在所有负载下没有延迟收益"——长上下文与批量尚未覆盖；②"已实测最大上下文容量提高1.9248倍"——该数字是存储布局的字节比，不是完整引擎的最大可运行Token容量比。**G5要求的最大容量实测仍未被这个字节比替代。**
- 容量账本在第二个容量点复现：BF16 9,437,184 B；INT8 4,718,592 B + scale 184,320 B = 4,902,912 B；比值同为1.924812，scale占整数数据3.906%、占总量3.759%。峰值allocated差4.33 MiB与池字节差4,534,272 B（4.3243 MiB）吻合。
- 上一轮39 Token下TTFT差为56.84 ms（2.14×）；本轮INT8代码路径未变、可作对照，等输入配对下差距降到0.469 ms，说明该差距来自BF16侧此前的参考写入与分页读回，不是存储精度。上一节"遗留问题"记的Decode侧约1.4 ms/Token净差，本轮同口径下降至0.076 ms（噪声级），归因为BF16 Decode此前走非融合`append`，**不是KV带宽**；这与2026-09-17台账"Decode非KV带宽受限"一致。
- **正确性**：用户报告`make build`、扩展符号自检、`python -m ampere_kv.paged_decode`（含BF16写入逐元素对照与六个INT8场景）、`--mode check --cache paged`（对HF `rtol=0, atol=0`）、`--mode cuda-check`、`--mode int8-check`全部通过；**原始输出未提供，此处记为用户报告通过**。benchmark中`与BF16固定长度序列一致=False`是预期观测，不是精度验收也不是内核错误。

### 2026-09-20 内部Decode路径移除逐层块号标量取回（待云端验证）

- **作者**：平台q（只改代码，本机无CUDA，未编译未运行）
- **动机**：`paged_decode_impl`每次调用对有效块表做`min().item()`/`max().item()`两次标量取回，Decode每Token 36层×2=72次CPU等待点（2026-09-17台账已记过该数字）。按Codex裁定：要消除的是这些逐层取回造成的等待，**不是物理块编号的边界约束**。
- **改动**：`paged_decode_kernel`增加物理块总数入参，在读取`physical`之后、任何K/V或scale访存之前用`CUDA_KERNEL_ASSERT`拦截`0 ≤ physical < 块总数`；`paged_decode_impl`增加`check_table`形参，把宿主min/max检查改为条件执行，其余形状/类型/设备/长度/块表长度检查仍无条件执行；新增`paged_decode_internal`与`paged_decode_int8_internal`两个内部入口并在`bindings.cpp`注册。
- **受检查入口保留**：`paged_decode`/`paged_decode_int8`行为不变，供独立算子检查与外部调用；`paged_decode.py`原有的非法块号测试仍打在受检查入口上，仍是可捕获的同步异常，"非法输入立即报错、进程还能继续做其他检查"的行为没有被悄悄改变。
- **错误处理语义差异**：内部入口"仍防止越界访问"**不等于**"仍保持原来的同步异常行为"。设备端断言属异步失败，通常在后续同步点才可见；发生后不能捕获异常继续复用同一CUDA上下文，后续CUDA调用都会失败。因此内部路径出现非法块表视为实现错误，失败后终止该运行；本轮不做可恢复GPU错误框架。
- **保护能力边界**：块号处于合法范围不代表属于当前请求；原来的min/max检查同样没有验证请求归属，归属仍由块池与块表生命周期保证。不得夸大新旧检查的保护能力。
- **BF16/INT8同时切换**：`runner.py`的`cuda_decode`分支两条精度都改用内部入口，不只优化一边。本轮不叠加块表常驻更新、CUDA Graph、Triton或其他内核优化，以免无法归因。
- **验证状态**：本地只做AST解析、`git diff --check`与CUDA声明/定义/绑定/参数静态核对，**未编译、未运行任何GPU测试**。新增`check_internal_guard`：因设备端断言会毒化上下文，探测在**独立子进程**执行（`--probe-guard bf16|int8`），父进程断言子进程到达探测点、未打印存活标记、非零退出、且stderr不含`illegal memory access`，用以区分"断言拦住了"与"保护没编进去、真的读越界"。**本机`.venv`无torch头文件，`CUDA_KERNEL_ASSERT`在实际构建配置中是否生效只能由该探测在云端确认**；若探测失败，属Codex裁定中"无法落实有效的访存前保护"，需再触发复核。
- **遗留**：同步减少后的实际收益未测量，需云端用固定输入（28 Token）跑`--mode benchmark --cache paged --kv-dtype int8`并与本轮45.165/38.969 ms同口径比较。G4/G5暂不关闭：最大容量实测、8192步精度统计、Triton融合、Custom Op/Meta/`opcheck`、V3同KV头协作与向量化加载仍未做。

### 2026-09-20 按 vLLM 风格精简防御性检查与注释（待云端验证）

- **作者**：平台q（只删校验与注释，未改算法、布局、量化规则或 benchmark 计时口径）
- **动机**：用户指出验证对照过多、代码难以人工审阅，要求以 vLLM 这类成熟引擎为标准——边界处校验、内部互相信任、热路径不为校验做宿主同步、测试不写进算子文件。精简前 7 个主要文件共 1932 行，其中 132 行检查、289 行注释（占 21%）。
- **补记上一轮实测**（推翻上一条"未测量"）：移除每层每 Token 的 `min().item()`/`max().item()` 后，TPOT 由 39.045/38.969 降到 37.321/37.178 ms（BF16/INT8，−1.724/−1.791 ms，约 −4.5%），即**每次设备同步约 24 µs**；这修正了此前从"移除每层 `position_ids.item()` 降 8.7 ms"反推的 242 µs/层——那次同时移除了逐层 KV 读回与重复检查，不是纯同步成本。设备端块号保护已由独立子进程探测确认在实际构建配置下生效（失败原因是断言而非非法访存）。该轮输入为 36 Token 而非 28，**跨轮 TTFT 不可直接相减**。
- **删除**：`paged_decode.py` 的内部入口设备端保护子进程探测框架（约 87 行，一次性使命已完成）；`kv_cache.py` 的 `_cached_attention` 16 个输入校验与 `ContiguousKVCache.append` 7 个；`paged_cache.py::append` 的形状/dtype/设备前置检查、以及融合分支中重复 CUDA 入口的支持范围检查；`block_pool.py` 的 `type(x) is not int` 式检查；`runner.py` 中与 argparse `choices=` 或 `main` 重复的 9 个 `generate_tokens` 校验、`apply_rope` 的 3 个逐层配置校验、`model_forward` 与 `decoder_layer_forward` 的 6 个内部不变量校验；`quantization.py::dequantize_kv` 的 5 个校验。`check_bf16_write` 由 42 行压到 27 行、`check_int8` 的逐 Token 物理写入循环改为 `get()` 一次性对照。
- **保留（最低代码量的安全保护）**：三处内核 `CUDA_KERNEL_ASSERT` 块号保护、`paged_decode_impl` 与两个写入入口的边界 `TORCH_CHECK`、量化规则本身（`write_scale`、最近偶数舍入、`[-127,127]`、零 scale 特例、FP16 范围拒绝）、块池耗尽与重复归还 raise、`locate` 的有效长度边界、`check_int8` 对 FP64 参考的数值对照与非法块号拒绝。
- **性能副产物**：`apply_rope` 的 3 个配置校验此前每层每步执行，删除后每 Token 少 108 次 Python 级判断；量级很小，未单独测量。
- **结果**：7 个文件 1932 → 1589 行（−343），检查 132 → 72，注释 289 → 187。**`csrc/paged_decode.cu` 的 30 条 `TORCH_CHECK` 本轮未动**——它们位于扩展入口边界，是 vLLM 也会保留的位置；且本机无法编译，重写表达式若出错要浪费一次云端构建，留待单独一轮。
- **验证状态**：本地只做 AST 解析与 `git diff --check`，**未编译、未运行**。需云端 `make build` 后重跑 `python -m ampere_kv.paged_decode`、`--mode check --cache paged`、`--mode cuda-check`、`--mode int8-check` 与 `--mode benchmark` 确认无回归。
- **行为变化**：非法输入现在更可能表现为 PyTorch/CUDA 的底层报错而非中文提示；内部路径出现非法块表仍按上一轮约定视为实现错误并终止运行。

### 2026-09-20 修复 check_int8 设备不匹配并记录精简轮云端结果

- **作者**：平台q
- **缺陷**：上一提交`6aa66d0`把`check_int8`的逐Token物理写入对照换成`cache.get()`一次性对照时，漏掉了原代码的`.cpu()`——`get()`返回CUDA张量，而CPU独立量化参考在CPU，云端报`values for attribute 'device' do not match: cuda:0 != cpu`。已改为`got_key.cpu()`/`got_value.cpu()`与CPU参考对照，对照强度不变。同轮的`check_bf16_write`因输入本身就在CUDA上，未受影响并已云端通过。
- **云端结果（`6aa66d0`，3090）**：`check_bf16_write` PASS；`--mode check --cache paged`对HF `rtol=0, atol=0` PASS。`--mode cuda-check`与`--mode int8-check`**未跑成**——执行者给出的命令漏了`--cache paged`，被`runner.py:547`的参数校验拒绝，**不是代码回归**。
- **性能**：BF16 TTFT 42.443 ms、TPOT 36.937 ms、总耗时1187.642 ms、吞吐26.944 Token/s；INT8为42.530/36.782/1182.873/27.053。TPOT较上一轮37.321/37.178再降约0.39 ms（约1%），量级与本轮删除的每Token约200次Python级校验相符（`apply_rope`三项×36层、`append`融合分支五条件×36层、`append_tokens`类型检查×36层，以及`decoder_layer_forward`与`model_forward`若干）；**未做单变量隔离，属推断归因**。
- **TTFT不可跨轮比较**：每层物理容量48 Token反推输入≤16 Token，上一轮为36 Token；用户未回传`输入 Token=`行，实际值未知。
- **容量账本第三个点复现**：BF16 7,077,888 B；INT8 3,538,944 + scale 138,240 = 3,677,184 B；比值仍为1.924812。峰值allocated差3.23 MiB与池字节差3,400,704 B（3.2434 MiB）吻合。
- **等路径结论再次成立**：TTFT差0.087 ms（方向与上一轮相反）、TPOT差0.155 ms，均不超过组内离散（TTFT 0.382/0.207 ms，TPOT 0.158/0.043 ms），n=3。
- **验证状态**：本地仅AST解析与`git diff --check`，未编译未运行。需云端重跑`python -m ampere_kv.paged_decode`（含修复后的`check_int8`）与`--mode cuda-check --cache paged`、`--mode int8-check --cache paged`。

### 2026-09-20 G4 检查点A：固定算子测量方式（待云端取基线）

- **作者**：平台q（只改`ampere_kv/paged_decode.py`的`benchmark`与`--benchmark`帮助文本）
- **动机**：V1（连续四维加载）需要可比基线，而原`--benchmark`只测BF16的受检查入口、不测INT8、只有墙钟一种口径；上一轮在vGPU上还暴露出中位数会掩盖20.6%的组内离散。没有固定测量方式就无法把V1的效果与噪声分开。
- **改动**：在现有`benchmark`内补齐BF16/INT8内部Decode入口的配对测量，BF16也从受检查入口切到内部入口（与模型生成路径一致，无宿主块号同步）；两种精度共用同一份随机输入、同一种倒序块号排列；保留64/256/1024三个长度；Q/K/V、量化、GQA展开与块表全部在计时外准备。新增`torch.cuda.Event`批量GPU时间窗口（同一批20次调用），汇总行同时给出墙钟与GPU窗口的中位数**及极差**。输出行数比改前更少（改前每组每次调用单独一行）。
- **计时范围声明**：墙钟包含Python/扩展调用、输出分配与组末等待；GPU窗口是同一批调用的Event区间，**仍包含GPU等待CPU派发的空隙，不是严格纯内核时间**。同一份数据反复读取可能受硬件缓存影响。
- **数值对照**：INT8用`cache.get()`还原的同一份量化数据做FP32 SDPA参考，量化误差不进入内核对照；容差沿用`rtol=0.01, atol=0.002`，**未放宽**。浮点加法顺序若变化必须靠对照暴露，不得靠调容差掩盖。
- **验证状态**：本地仅AST解析与`git diff --check`，未编译未运行。需在**3090**上跑`python -m ampere_kv.paged_decode --benchmark`取得当前内核（V0）基线；本轮只改Python，若该容器已构建过含内部入口绑定的扩展（`79e5ea6`之后）则无需`make build`。**没有这组基线之前不形成任何V1性能结论。**
- **遗留**：检查点B（V1实现，只改`csrc/paged_decode.cu`）另开pre，不与本轮合并归因。G4/G5仍未关闭，本轮不进入G6、不实现V3。

### 2026-09-20 V0 算子基线（RTX 3090）+ G4 检查点B：V1 连续四维加载

- **作者**：平台q（改`paged_decode_kernel`与两个打包载入助手；未改量化、容差、段长、Attention公式与benchmark）
- **V0 基线**（`--benchmark`，3090 24576MiB，提交`1a82641`，每组20次×3组取中位数，括号内为组内极差，单位µs/调用；格式 墙钟/GPU窗口）：
  - 长度64：SDPA连续 19.729(3.471)/18.688(1.741)；CUDA BF16内部 12.734(0.347)/12.083(0.206)；CUDA INT8内部 17.731(0.117)/17.050(0.102)
  - 长度256：SDPA连续 25.095(1.473)/24.320(0.616)；CUDA BF16内部 15.471(0.086)/14.848(0.102)；CUDA INT8内部 20.481(0.058)/19.866(0.102)
  - 长度1024：SDPA连续 37.443(0.850)/36.762(0.358)；CUDA BF16内部 19.818(0.312)/19.149(0.154)；CUDA INT8内部 25.446(0.456)/24.781(0.461)
  - 计时外数值对照最大绝对误差：BF16/连续SDPA 0.00390625、0.001953125、0.0009765625；INT8/同量化数据 0.0019514561、0.00093340874、0.00048160553。均在`rtol=0.01, atol=0.002`内且余量很大，误差量级与输出幅度的BF16单ulp一致。
- **基线三条读数**：①三条CUDA路径组内极差仅0.058~0.461µs（0.6~2.3%），测量可用；唯一不稳的是SDPA@64（极差3.471µs=17.6%）。②GPU窗口≈墙钟减0.62~1.04µs，说明CPU派发跟得上，测得的是GPU时间主导，内核改动可显示。③**INT8比BF16慢一个近似恒定的量：+4.97/+5.02/+5.63µs**，而长度64→1024是16倍工作量。按warp折算为38.8/9.8/2.75 ns/warp，明显非线性，且INT8读的字节只有BF16一半——**这是固定开销/延迟特征，不是带宽特征**。length=64时全网格128 warp摊到82个SM只有约1.6 warp/SM，占用率远低于SM容量，依赖链长度直接成为墙钟。
- **V1 动机（由上述③推出）**：收益在依赖链不在带宽。把每线程负责的维度由`lane, lane+32, lane+64, lane+96`（跨四个scale组）改为**连续四维**`4*lane..4*lane+3`（同一组），于是K scale广播从循环内4次shuffle降为循环外1次，且四条窄载入合并为一条宽载入。
- **改动**：新增`load4`两个重载与`Bf16x4`/`I8x4`打包视图（BF16 8字节经`float2`，INT8 4字节经`int32_t`），行首为128元素倍数故对齐天然满足；query与循环内K、V各一次打包载入；INT8广播源为`lane/8`（**保持lane0..3装载**，不改成0/8/16/24）；`shared`写入、跨warp合并读取、`partials`与输出四处索引统一改为`4*lane+i`，**自然维度布局与`merge_decode_kernel`接口均未变**；保留访存前块号断言。
- **预期数值影响**：每个线程聚合的四个维度不同、树形归约的分组也随之改变，**FP32加法顺序确实变了**，差异应在FP32末位、经BF16舍入后基本一致。容差、量化规则与段长一律未动；若云端对照超差，按方案不得放宽容差，直接回报。
- **验证状态**：本机无CUDA，未编译未运行。已静态核对五处`4*lane+i`与三处`load4`一致、无`lane + i*32`残留、`if constexpr`下全warp参与shuffle无发散风险、小端下`value[0]`对应最低维。**云端需一次性确认**宽载入指令与寄存器占用（不增加常驻框架），并跑`--benchmark`与`paged_decode`默认检查。
- **仍未满足的G4 PASS项**：方案要求"相对 **PyTorch Gather Reference** 有稳定正收益"，当前参考是连续内存SDPA且物化了GQA展开（4倍数据），不是从分页布局gather的参考。该口径差异交主审定夺，本轮不自行改基线定义。

### 2026-09-20 V1 实测结果（3090）：连续四维打包载入有正收益，方向与长度相关

- **作者**：平台q（本轮只记录，未改代码；被测代码为提交`cac400c`，V0基线为`1a82641`）
- **A/B（GPU窗口中位数，µs/调用；括号内为组内极差）**：

| 长度 | BF16 V0 | BF16 V1 | Δ | INT8 V0 | INT8 V1 | Δ |
| --- | --- | --- | --- | --- | --- | --- |
| 64 | 12.083(0.206) | 11.981(0.154) | **−0.102（−0.8%）** | 17.050(0.102) | 16.179(0.102) | **−0.871（−5.1%）** |
| 256 | 14.848(0.102) | 14.541(0.102) | **−0.307（−2.1%）** | 19.866(0.102) | 18.790(0.053) | **−1.076（−5.4%）** |
| 1024 | 19.149(0.154) | 18.381(0.205) | **−0.768（−4.0%）** | 24.781(0.461) | 22.784(0.307) | **−1.997（−8.1%）** |

  墙钟口径同向且量级一致（BF16 −0.155/−0.291/−0.832，INT8 −0.919/−0.994/−2.017 µs），两种计时不是互相独立的证据但方向吻合。**可信度分级**：除 BF16@64 外的五格，差值都大于两侧极差，方向成立；**BF16@64 的 −0.102 µs 小于其极差（0.206/0.154），只能记为"无可测变化"，不得写成收益**。
- **收益结构**：INT8 的改善在每个长度上都大于 BF16（−5.1%/−5.4%/−8.1% 对 无可测变化/−2.1%/−4.0%）。这与预期一致——V1 给两条路径的共同改动是"四条窄载入并成一条宽载入"，而**只有 INT8 额外拿到 K scale 广播从循环内4次shuffle减为1次**；BF16@64 看不出变化而 INT8@64 能看出，正说明短上下文下 BF16 只剩那点载入合并、被 launch 开销摊薄，而 INT8 还有 shuffle 链可省。
- **V0 那个近似恒定的 INT8 惩罚被压缩但没消失**：INT8−BF16 由 +4.967/+5.018/+5.632 µs 降到 **+4.198/+4.249/+4.403 µs**，压缩 15.3%~21.8%。**剩下的 ~4.2-4.4 µs 与长度无关，说明它不是 scale 广播链**（那条已被 V1 处理掉），另有来源。这是下一步的定位目标，不再猜：可能是每 Token 两次谓词化 scale 载入（`lane<4` 取 K、`lane==0` 取 V）与 INT8→FP32 转换链。
- **向量化已确认，不是只换了索引**：`cuobjdump -sass` 显示 **`LDG.E.U8`/`LDG.E.S8` 已全部消失**，代之以 `LDG.E`（32位）47 条与 `LDG.E.64` 10 条；即 INT8 的四维合成一次 32 位读、BF16 合成一次 64 位读。`cuobjdump -res-usage`：BF16 `REG:48`、INT8 `REG:50`、`SHARED:2080`、`STACK:0 LOCAL:0`（无寄存器溢出）。**V0 的寄存器数从未测过，所以不断言"寄存器未增加"**；当前值不构成占用率限制（128线程块下 8 块可驻留）。
- **数值逐位不变**：`--benchmark` 三个长度的最大绝对误差（BF16 `0.00390625/0.001953125/0.0009765625`，INT8 `0.0019514561/0.00093340874/0.00048160553`）与 `paged_decode` 六个 INT8 场景的误差（`0/0.22774812/0.0034842584/0/0.2496569/0.24139731`）**与 V0 逐位相同**。此前预警的 FP32 加法顺序改变没有 observable 影响：扰动量级约 2⁻²³，而 BF16 输出间隔约 2⁻⁸，相差约四万倍跨不过舍入边界。容差、量化规则、段长均未改，**不是靠放宽阈值过关**。误差与 V0 完全一致同时计时变快，也排除了"构建缓存复用旧二进制"的假通过。
- **256 行的处理过程如实记录**：第一次跑 `--benchmark` 时该行被外部干扰——SDPA 参考中位数从 24.320 涨到 53.555 µs、极差 30.106 µs，而 SDPA 根本不经过被改内核，故判定为机器受扰，**该次整行弃用**。重跑后 SDPA 24.830(0.768) 与 V0 的 24.320(0.616) 相差 0.51 µs、落在各自极差内，作为不经被改内核的**同轮对照组**证明机器状态可比，上表的 256 行取自重跑。
- **未验证（不得当作通过）**：①**V1 只重跑了算子级检查与 benchmark，模型级对照（`--mode check --cache paged`、`--mode cuda-check --cache paged`，两者都必须显式带 `--cache paged`）是在 `1a82641` 上做的，`cac400c` 之后未重跑**——V1 改的正是模型 Decode 用的内核，这条必须补；②内核级 A/B 未折算成端到端收益，模型级 TPOT 变化需在 3090 上另测，不能由本表推断；③未测长上下文（>1024）；④G4 的 gather reference、V3、Custom Op/Meta/`opcheck`、NSYS 仍未做，G4/G5 不关闭。
- **下一步候选（不在本轮结论内）**：定位残留的 ~4.3 µs 长度无关开销；以及按方案顺序推进 V3（同 KV 头 4 个 Query 头共享 K/V 加载，即预注册假设 H2，当前 K/V 被重复读 4 次）。

### 2026-09-21 V3 原型：同组四个 Query 头共享一次 K/V 载入（已实现，未编译未测量）

- **作者**：平台q
- **动机**：V1 之后 INT8−BF16 还剩约 4.3 µs 且与长度无关，同时同一 KV 头的 K/V 仍被四个 Query 头各读一遍（预注册假设 H2）。V3 把一整组四个 Query 头放进同一个线程块，验证寄存器内复用能否换来正收益。
- **分工与复用**：`blockIdx.x` 由 Query 头号改为 GQA 组号，块内仍是四个 warp 交错处理段内 Token，每线程持有连续四维 × 四个头（16 个 FP32 累加器与 16 个 query 分量）。每 Token 的块表读取、K 行与 V 行载入、两份 scale 广播和 INT8 反量化都只做一次，四个头各自点积、各自 warp 归约、各自维护在线 softmax 的 max/分母/累加器，**softmax 不跨头混合**。每头的算术次序与 V1 逐项相同，所以结果差异只应来自编译器取舍。
- **与 V1 的关键差别**：V1 内核**一行未改**，继续作 A/B 基线和非 4 倍 GQA 的回退；段长 64、partials 布局 `[Query头, 段, 130]`、跨段 merge 内核、stream、有效长度与尾段语义全部不变。新增 `paged_decode_v3_kernel<KV>`，BF16/INT8 仍共用模板。
- **预期代价（未验证，须云端确认）**：静态共享内存 2080→8320 字节；每线程多 12 个 query 与 12 个累加器分量；**网格横坐标 32→8，块数降为四分之一**，短上下文可能因并行度不足反而变慢。不预设 DRAM 流量除以四就等于提速。
- **选择方式与保护**：四个入口新增末位 `v3` 参数，默认 `false` 仍是 V1，未测量前不替换默认路径，也没有长度阈值自动选择。`v3=true` 时宿主先检查 `q_heads == kv_heads * 4`，**不支持的配置直接报错而不是静默退回 V1**，这样"跑通了"本身就证明用的是四头内核；内核内 K/V 与 scale 访存前的块号断言保留。模型侧加 `--decode-kernel {v1,v3}`，`cuda-check`/`int8-check`/`benchmark --cache paged`/`generate --kv-dtype int8` 会实际调用 V3（`main` 拒绝把 v3 用在不会走 CUDA Decode 的组合上，避免把 SDPA 或 V1 的结果当成 V3）。
- **验证状态**：本机无 CUDA，**未编译、未运行任何检查或测量**。已做 AST 解析、`git diff --check` 与静态索引核对。**云端待执行**：默认算子检查（新增 4 倍 GQA 的 V3 对照与非 4 倍请求被拒绝两项）、`--benchmark`（长度 64/256/1024/4096，同轮内 V1/V3 交替，报中位数与极差）、一次性 `cuobjdump -res-usage` 看寄存器/共享内存/是否溢出、`--mode check --cache paged`（补上轮 V1 模型级未重跑）、`cuda-check --cache paged --decode-kernel v3` 与 `int8-check --cache paged --decode-kernel v3`。正确性通过前不谈性能；算子变快不等于模型 TPOT 提升。
- **本轮行数如实报告**：任务书未给行数预算，我自定的 240 行被实测 340 行超出，其中 V3 内核本体 137 行。原因是把"新增内核 + 宿主派发 + 四个入口 + 算子 A/B + 模型接线"的成本估低了，不是塞进了额外验证框架。用户授权放行并上调基线预算，约定在后续版本迭代中逐步删减多余行；本轮代码范围未因预算调整而扩大。
- **仍未满足的 G4 PASS 项**：PyTorch Gather Reference 口径未定，模型级 V3 结果待云端，长上下文仅覆盖到算子 4096；G4/G5 不关闭。
