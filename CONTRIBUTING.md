# AmpereKV 贡献规范

## 修改范围

每次修改只解决一个明确问题，并说明它属于当前 Gate 的哪一项交付。没有通过
G0/G1 前，不提前加入 Paged Decode、INT8 KV、调度器或 CUDA Graph 实现。

## 代码注释

- 源码注释统一使用中文；
- 注释优先解释数据布局、边界条件、不变量和设计原因；
- 不使用“显然”“简单”等词替代必要的推导；
- CUDA/Triton Kernel 必须能够按数据流逐行讲解；
- 修改核心 Kernel 时同步更新测试、输入矩阵和负收益边界。

## 正确性与性能

- 正确性测试先于性能优化；
- 不以最终生成文本“看起来正确”代替 Logits 或张量级对齐；
- 不使用单次最小耗时作为正式结果；
- 性能数据必须绑定 Commit、环境、Shape、dtype、Warm-up 和重复次数；
- 没有真实运行证据时，不填写加速数字或宣称 Gate 已通过。

## 提交前检查

1. 只暂存本次修改涉及的明确文件；
2. 使用 git diff --cached 检查实际提交内容；
3. 检查个人信息、凭据、模型权重、私有数据和原始 Profiler 大文件；
4. 运行当前环境能够执行的测试；
5. 在提交说明中写明尚未执行的 GPU 验证。
